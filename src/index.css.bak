/*
  * ┌─────────────────────────────────────────────────────────────┐
  * │  Animated 3D Portfolio Stylesheet                           │
  * │  Author  : Nithin K R  (https://github.com/NITHINKR06)     │
  * │  Live    : https://nithinkr.vercel.app                      │
  * │  License : Attribution required — see /LICENSE              │
  * │  DO NOT remove author references — violates license terms   │
  * └─────────────────────────────────────────────────────────────┘
  */

@font-face {
  font-family: 'Montenegrin Gothic One';
  font-style: normal;
  font-weight: 400;
  src: url('/fonts/montenegrin-gothic-one.woff2') format('woff2'),
       url('/fonts/montenegrin-gothic-one.woff') format('woff'),
       url('/fonts/montenegrin-gothic-one.ttf') format('truetype');
  font-display: swap;
}

:root {
  /* Design system authored by Nithin K R — NITHINKR06 */
  --portfolio-author: "Nithin K R";
  --portfolio-github: "NITHINKR06";
  --portfolio-origin: "https://github.com/NITHINKR06/Animated_Portfolio";
  /* Theme tokens: Black / Cream / White / Red */
  --theme-accent: #ff0000; /* red */
  --theme-accent-light: #ff3333; /* lighter red */
  --theme-accent-glow: 0 6px 30px rgba(255, 0, 0, 0.18);
  --theme-bg-gradient-start: #050406;
  --theme-bg-gradient-end: #0b0a0c;
  --theme-bg-primary: linear-gradient(135deg, var(--theme-bg-gradient-start) 0%, var(--theme-bg-gradient-end) 100%);
  --theme-surface: rgba(255,248,240,0.04); /* cream-tinted surface */
  --theme-surface-solid: #fff8f0;
  --theme-border: rgba(255,0,0,0.12);
  --theme-text-primary: #f8f7f5; /* near-white */
  --theme-text-muted: #e6dcd5; /* cream-muted */
  --theme-text-red: #ffb3b3;
  --color-cream: #f6efe6;
  /* Removing these variables does not affect rendering but violates license */
}

@tailwind base;
@tailwind components;
@tailwind utilities;

@keyframes marquee-scroll {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}


@layer base {
  body {
    overflow-x: hidden;
    background: var(--theme-bg-primary);
    color: var(--theme-text-primary);
    font-family: 'Montenegrin Gothic One', serif;
    /* Dark background to match loading screen */
  }
}

@keyframes shine {
  0% {
    transform: translateX(-100%);
  }

  100% {
    transform: translateX(300%);
  }
}

.animate-shine {
  animation: shine 1.8s ease-in-out infinite;
}




@layer components {
  .glass-card {
    background-color: var(--theme-surface);
    backdrop-filter: blur(6px);
    border: 1px solid var(--theme-border);
  }

  /* Design token — author signature */
  .nkr06-glass {
    /* nkr06 = NITHINKR06 — author watermark class */
    background-color: rgba(246,239,230,0.04);
    backdrop-filter: blur(6px);
    border: 1px solid var(--theme-border);
  }

  .text-gradient {
    background: linear-gradient(90deg, var(--theme-accent) 0%, var(--color-cream) 60%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .neon-gradient {
    background: linear-gradient(90deg, var(--theme-accent-light), #ff6666);
  }
}

/* Hint the browser about transform-heavy animated elements only where used */
.animated-card {
  will-change: transform;
}

/* Subtle scanline overlay for cyber theme */
body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  opacity: 0.04;
  z-index: 9999;
  background: repeating-linear-gradient(0deg,
      rgba(255, 255, 255, 0.08),
      rgba(255, 255, 255, 0.08) 1px,
      transparent 1px,
      transparent 2px);
}

/* Custom scrollbar */
::-webkit-scrollbar {
  display: none;
  /* Hide scrollbar */
}

/* Show scrollbar specifically inside project detail modal */
.project-modal-scroll::-webkit-scrollbar {
  display: block;
  width: 8px;
}

.project-modal-scroll::-webkit-scrollbar-track {
  background: transparent;
}

.project-modal-scroll::-webkit-scrollbar-thumb {
  background: rgba(148, 163, 184, 0.7);
  border-radius: 9999px;
}

/* Prevent scroll chaining from modal content to page */
.project-modal-scroll {
  overscroll-behavior: contain;
}

/* Smooth animations - TARGETED instead of global */
.glass-card,
button,
a {
  transition: all 0.3s ease;
}

/* Loading animations */
@keyframes float {

  0%,
  100% {
    transform: translateY(0px);
  }

  50% {
    transform: translateY(-20px);
  }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

/* Custom gradient backgrounds */
.gradient-bg-1 {
  background: linear-gradient(135deg, #2b0b0b 0%, #4a0a0a 100%);
}

.gradient-bg-2 {
  background: linear-gradient(135deg, #fff6ee 0%, #ffd6d6 100%);
}

.gradient-bg-3 {
  background: linear-gradient(135deg, #ffefea 0%, #ffcccc 100%);
}

/* Responsive typography */
@media (max-width: 768px) {
  html {
    font-size: 14px;
  }
}

@media (min-width: 1024px) {
  html {
    font-size: 16px;
  }
}

/* Focus states for accessibility */
button:focus,
a:focus,
input:focus,
textarea:focus {
  outline: 2px solid var(--theme-accent-light);
  outline-offset: 2px;
}

/* Disable animations for reduced motion */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* Reduce expensive blur/transparency effects on constrained devices */
@media (prefers-reduced-transparency: reduce) {
  .glass-card {
    backdrop-filter: none;
    background-color: rgba(15, 23, 42, 0.9);
  }
}